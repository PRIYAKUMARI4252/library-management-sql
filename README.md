# library-management-sql
SQL-based Library Management System project
# Library Management System (SQL)

This project contains SQL scripts for a simple Library Management System.  
It includes database schema, sample data, and common queries.

## 📂 Files Included
- **library_schema.sql** – Creates tables (Books, Members, Issue, Return)
- **library_insert.sql** – Inserts sample data
- **library_queries.sql** – Query examples

## 💡 Technologies Used
- MySQL / PostgreSQL / SQL Server (any database)

## 🚀 How to Run
1. Create a database
2. Run `library_schema.sql`
3. Run `library_insert.sql`
4. Execute queries from `library_queries.sql`
